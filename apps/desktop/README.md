"""Desktop client (placeholder).

Planned stack:
- Tauri 2 shell (macOS + Linux)
- Reuse the Vite/React UI from `apps/web`
- Python sidecar that runs `clonebins_core` (same pipeline as the CLI)

No Tauri/Rust/React code in this folder yet. See `docs/architecture.md`.
"""
